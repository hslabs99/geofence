/** tbl_settings.type for fence timing keys below (safe to import from client components). */
export const GPS_FENCE_SETTINGS_TYPE = 'System';

/** ENTER/EXIT tagging: minimum seconds a fence/null state must persist (tbl_settings.settingname). */
export const GPS_STD_TIME_NAME = 'GPSstdTime';

/** Steps+ (GPS+): buffer distance in metres. */
export const GPSPLUS_GROWTH_NAME = 'GPSplusGrowth';

/** Steps+ (GPS+): minimum buffered stay duration in seconds. */
export const GPSPLUS_TIME_NAME = 'GPSplusTime';
