/** tbl_settings keys for trailermode TT vs T from loadsize (Admin → Settings). */

export const VWORK_TT_LOAD_SIZE_SETTINGS_TYPE = 'System';

/** Stored setting name (no spaces) — display label in UI: "TT Load Size". */
export const VWORK_TT_LOAD_SIZE_SETTING_NAME = 'TTLoadSize';

export const VWORK_TT_LOAD_SIZE_DEFAULT = 25.5;
